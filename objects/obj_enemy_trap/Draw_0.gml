draw_self();

/*
if(explosion > 0){
	//draw_sprite_ext(spr_landmine_hitbox,0,x,y,explosion,explosion,0,c_white,1);
	
	i++;
	explosion_direction_x = lengthdir_x(i,explosion_direction);
	explosion_direction_y = lengthdir_y(i,explosion_direction);
	
	draw_circle(x+x_explosion_offset+explosion_direction_x,y+y_explosion_offset+explosion_direction_y,explosion,false);
	x_explosion_offset = random_range(-3,3);
	y_explosion_offset = random_range(-3,3);
	explosion += explosion_expand;
	//explosion_expand += 4;
	if(explosion > sprite_get_width(spr_landmine_hitbox)){
	explosion_expand = clamp(explosion_expand-6, 0, explosion);	
	}
}
*/
